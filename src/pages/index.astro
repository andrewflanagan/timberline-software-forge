---
const navLinks = [
  { href: "#services", label: "Services" },
  { href: "#process", label: "How We Work" },
  { href: "#about", label: "About" },
  { href: "#contact", label: "Contact" },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Timberline Code Forge • System Software & Autonomy Consulting</title>
    <meta
      name="description"
      content="Timberline Code Forge is a Pacific Northwest consultancy focused on system software, autonomy, and robotics-focused back-end development."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Poppins:wght@600;700&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <!-- HEADER -->
    <header class="site-header">
      <div class="container header-inner">
        <a href="/" class="brand">
          <div class="brand-mark">TCF</div>
          <div class="brand-text">
            <span class="brand-title">Timberline Code Forge</span>
            <span class="brand-tagline">system software • autonomy • craft</span>
          </div>
        </a>
    
        <nav class="nav">
          {navLinks.map((item) => (
            <a href={item.href} class="nav-link">{item.label}</a>
          ))}
          <a href="#contact" class="nav-cta">Start a Project</a>
        </nav>
      </div>
    </header>

    <main>

      <!-- HERO (logo as full-bleed blurred background) -->
      <section class="hero" id="top">
        <!-- Blurred, extended background based on the logo -->
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
      
        <div class="container hero-inner">
          <!-- Crisp logo on top -->
          <img
            src="/images/timberline-logo.png"
            alt="Timberline Code Forge logo with Mount Rainier and forest circuit"
            class="hero-logo"
            width="800"
            height="800"
            loading="eager"
          />
      
          <p class="pill pill-hero">Forged in the Pacific Northwest</p>
      
          <h1 class="hero-title">
            System software and autonomy, built to survive the field.
          </h1>
      
          <p class="hero-subtitle">
            Timberline Code Forge helps teams design and implement back-end and
            on-vehicle software for robots, sensors, and mission-critical systems.
          </p>
      
          <div class="hero-actions">
            <a href="#contact" class="btn-primary">Start a Project</a>
            <a href="#services" class="btn-ghost">Explore Services</a>
          </div>
        </div>
      </section>

      <!-- SERVICES -->
      <section class="section" id="services">
        <div class="container">
          <header class="section-header">
            <h2>Back-end and autonomy services</h2>
            <p>
              We focus on the parts that are hardest to fake: real systems,
              moving hardware, and software that has to behave when conditions
              aren’t ideal.
            </p>
          </header>

          <div class="grid">
            <article class="card">
              <h3>System &amp; Platform Software</h3>
              <p>
                Design and implementation of services, daemons, and middleware
                for embedded and edge devices. Clean, testable code around the
                operating system, not bolted on top of it.
              </p>
              <ul class="chip-list">
                <li>C / C++ / Rust</li>
                <li>Linux &amp; RTOS</li>
                <li>Messaging &amp; middleware</li>
              </ul>
            </article>

            <article class="card">
              <h3>Autonomy &amp; Mission Logic</h3>
              <p>
                Behavior engines, state machines, and mission orchestration for
                uncrewed systems. We bridge the gap between algorithms and
                real-world deployment.
              </p>
              <ul class="chip-list">
                <li>Behavior trees &amp; planners</li>
                <li>Robotics frameworks (ROS2-style)</li>
                <li>Simulation &amp; testing harnesses</li>
              </ul>
            </article>

            <article class="card">
              <h3>Sensor &amp; Actuator Integration</h3>
              <p>
                Drivers, adapters, and data flows that make your hardware usable:
                from low-level protocols to well-structured messages ready for
                autonomy and analytics.
              </p>
              <ul class="chip-list">
                <li>Sensors &amp; effectors</li>
                <li>Serial / CAN / custom buses</li>
                <li>Time sync &amp; data fusion</li>
              </ul>
            </article>

            <article class="card">
              <h3>DevOps for Robotics Backends</h3>
              <p>
                CI/CD, packaging, and observability tuned for fleets and field
                systems—where connectivity is intermittent and logs matter.
              </p>
              <ul class="chip-list">
                <li>Build &amp; packaging pipelines</li>
                <li>Container &amp; artifact registries</li>
                <li>Telemetry &amp; log aggregation</li>
              </ul>
            </article>
          </div>
        </div>
      </section>

      <!-- PROCESS -->
      <section class="section section-alt" id="process">
        <div class="container">
          <header class="section-header">
            <h2>A process designed for complex systems</h2>
            <p>
              We’ve been on the hook for systems that have to work—in the field,
              underwater, and in production. Our process reflects that reality.
            </p>
          </header>

          <ol class="steps">
            <li>
              <h3>1. System &amp; domain discovery</h3>
              <p>
                We map your current architecture, interfaces, and constraints:
                platforms, buses, comms links, and mission profiles.
              </p>
            </li>
            <li>
              <h3>2. Architecture &amp; interface design</h3>
              <p>
                We propose clear component boundaries, message contracts, and
                deployment patterns that your team can understand and maintain.
              </p>
            </li>
            <li>
              <h3>3. Implementation &amp; integration</h3>
              <p>
                Short implementation cycles with hardware-in-the-loop where
                possible, including drivers, services, and tests that prove
                everything works together.
              </p>
            </li>
            <li>
              <h3>4. Verification, handoff &amp; support</h3>
              <p>
                Bench and field-focused testing, documentation, and knowledge
                transfer. We can stay involved or transition cleanly to your team.
              </p>
            </li>
          </ol>
        </div>
      </section>

      <!-- ABOUT -->
      <section class="section" id="about">
        <div class="container about-grid">
          <div>
            <header class="section-header left">
              <h2>Rooted in autonomy and systems work</h2>
              <p>
                Timberline Code Forge is a Pacific Northwest consultancy focused
                on the type of software that sits closest to the hardware and
                carries the most risk when it goes wrong.
              </p>
            </header>
            <p>
              Our background spans mission-critical platforms, uncrewed vehicles,
              and long-lived services. We’re comfortable working where autonomy,
              controls, and infrastructure overlap.
            </p>
            <p>
              We bring that experience to every project: make behavior explicit,
              keep interfaces boring, and design for operations from day one.
            </p>
          </div>

          <aside class="about-aside">
            <h3>Where we’re most useful</h3>
            <ul>
              <li>Back-end &amp; on-vehicle software for robotics</li>
              <li>New autonomy capabilities on existing platforms</li>
              <li>Refactoring tangled mission logic into clearer designs</li>
              <li>Improving build, test, and deployment for system software</li>
            </ul>
          </aside>
        </div>
      </section>

      <!-- CONTACT -->
      <section class="section section-alt" id="contact">
        <div class="container contact-inner">
          <div>
            <header class="section-header left">
              <h2>Tell us about your system</h2>
              <p>
                Fleet of vehicles, a single prototype, or a back-end that keeps
                everything connected—if it involves real hardware and real
                constraints, we’d love to hear about it.
              </p>
            </header>
          </div>

          <form class="contact-form">
            <div class="field">
              <label for="name">Name</label>
              <input id="name" name="name" type="text" required />
            </div>
            <div class="field">
              <label for="email">Email</label>
              <input id="email" name="email" type="email" required />
            </div>
            <div class="field">
              <label for="company">Organization (optional)</label>
              <input id="company" name="company" type="text" />
            </div>
            <div class="field">
              <label for="message">What are you working on?</label>
              <textarea
                id="message"
                name="message"
                rows="4"
                placeholder="Short description of your platform, current challenges, and timelines…"
              ></textarea>
            </div>
            <button type="submit" class="btn-primary full">
              Send message
            </button>
            <p class="contact-note">
              Or email us directly at
              <a href="mailto:hello@timberlinecodeforge.com">
                hello@timberlinecodeforge.com
              </a>
            </p>
          </form>
        </div>
      </section>
    </main>

    <footer class="site-footer">
      <div class="container footer-inner">
        <p>© {new Date().getFullYear()} Timberline Code Forge. All rights reserved.</p>
        <p class="footer-small">Crafted at the edge of the evergreens.</p>
      </div>
    </footer>
  </body>

  <style>
    :root {
      --evergreen-700: #163a33;
      --evergreen-600: #205044;
      --evergreen-400: #3c5c4a;
      --moss-200: #90a594;
      --ember-500: #e06a2d;
      --copper-500: #b55a2b;
      --snow-cream: #f1ede2;
      --canopy-900: #0d2a26;
      --canopy-800: #112f2a;
      --text-strong: #e7efea;
      --text-default: #cfdbd6;
      --text-muted: #9baaa4;
      --border: #274340;
      --card: #122826;
      --radius-lg: 18px;
      --shadow-lg: 0 18px 40px rgba(0, 0, 0, 0.45);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto,
        "Helvetica Neue", Arial, sans-serif;
      color: var(--text-default);
      background: radial-gradient(
          120% 120% at 50% -10%,
          var(--evergreen-600),
          var(--canopy-900) 60%
        );
      -webkit-font-smoothing: antialiased;
      text-rendering: optimizeLegibility;
    }

    img {
      max-width: 100%;
      display: block;
    }

    .container {
      max-width: 1120px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    /* HEADER */

    .site-header {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(10px) saturate(140%);
      background: rgba(13, 42, 38, 0.7);
      border-bottom: 1px solid var(--border);
    }

    .header-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 1.5rem;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 0.9rem;
      text-decoration: none;
      color: inherit;
    }
    
    .brand-mark {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: Poppins, Inter, system-ui, sans-serif;
      font-weight: 700;
      font-size: 0.9rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #1b0d06;
      background: radial-gradient(
        140% 140% at 20% 10%,
        var(--ember-500),
        var(--copper-500)
      );
      box-shadow: var(--shadow-lg);
    }
    
    .brand-text {
      display: flex;
      flex-direction: column;
      gap: 0.15rem;
    }
    
    .brand-title {
      font-family: Poppins, Inter, system-ui, sans-serif;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      font-size: 0.9rem;
      color: var(--snow-cream);
    }
    
    .brand-tagline {
      font-size: 0.7rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--moss-200);
    }


    .nav {
      display: flex;
      align-items: center;
      gap: 1.25rem;
      font-size: 0.9rem;
    }

    .nav-link {
      color: var(--moss-200);
      text-decoration: none;
      font-weight: 500;
    }

    .nav-link:hover {
      color: var(--snow-cream);
    }

    .nav-cta {
      background: var(--ember-500);
      color: #120c07;
      padding: 0.5rem 0.9rem;
      border-radius: 999px;
      text-decoration: none;
      font-weight: 700;
      box-shadow: 0 10px 24px rgba(224, 106, 45, 0.4);
      font-size: 0.9rem;
    }

    .nav-cta:hover {
      background: var(--copper-500);
    }

    /* HERO with blurred logo background */
    
    .hero {
      position: relative;
      min-height: 80vh;
      padding: 4.5rem 0 3.5rem;
      overflow: hidden;
      display: flex;
      align-items: center;
    }
    
    .hero-bg {
      position: absolute;
      inset: 0;
      background-image: url("/images/timberline-logo.png");
      background-size: cover;
      background-position: center;
      filter: blur(14px);
      transform: scale(1.15);          /* zoom a bit so blurred edges don’t show */
      opacity: 0.75;
      pointer-events: none;
    }
    
    /* darker gradient so text stays readable */
    .hero-overlay {
      position: absolute;
      inset: 0;
      background:
        radial-gradient(
          80% 80% at 50% 0%,
          rgba(11, 39, 34, 0.1),
          transparent 55%
        ),
        linear-gradient(
          180deg,
          rgba(11, 39, 34, 0.05) 0%,
          rgba(11, 39, 34, 0.85) 70%,
          rgba(11, 39, 34, 1) 100%
        );
      pointer-events: none;
    }
    
    .hero-inner {
      position: relative;
      z-index: 1;
      max-width: 720px;
      margin: 0 auto;
      text-align: center;
    }
    
    .hero-logo {
      width: min(360px, 70vw);
      height: auto;
      margin: 0 auto 1.4rem;
      display: block;
      filter: drop-shadow(0 18px 40px rgba(0, 0, 0, 0.55));
    }
    
    /* reuse / tweak existing pill + typography */
    
    .pill {
      display: inline-block;
      padding: 0.3rem 0.7rem;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: rgba(10, 20, 20, 0.9);
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--moss-200);
    }
    
    .pill-hero {
      margin-bottom: 0.5rem;
    }
    
    .hero-title {
      font-family: Poppins, Inter, system-ui, sans-serif;
      font-size: clamp(2rem, 4.4vw, 3rem);
      line-height: 1.12;
      margin: 0.3rem 0 0.75rem;
      color: var(--snow-cream);
    }
    
    .hero-subtitle {
      font-size: 1.02rem;
      max-width: 40rem;
      margin: 0 auto 1.6rem;
      color: var(--moss-200);
    }
    
    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.85rem;
    }
    
    /* buttons reuse your existing .btn-primary / .btn-ghost styles */
    
    @media (max-width: 640px) {
      .hero {
        padding: 3.5rem 0 2.6rem;
        min-height: 70vh;
      }
    
      .hero-logo {
        width: min(280px, 75vw);
        margin-bottom: 1.1rem;
      }
    }

    .btn-primary,
    .btn-ghost {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.75rem 1.15rem;
      border-radius: 0.75rem;
      font-weight: 600;
      text-decoration: none;
      font-size: 0.95rem;
    }

    .btn-primary {
      background: linear-gradient(
        90deg,
        var(--ember-500),
        var(--copper-500)
      );
      color: #1d0f06;
      box-shadow: 0 14px 32px rgba(224, 106, 45, 0.45);
    }

    .btn-primary:hover {
      filter: brightness(1.03);
    }

    .btn-ghost {
      border: 1px solid var(--border);
      background: linear-gradient(180deg, #16312d, #0f1b1c);
      color: var(--snow-cream);
    }

    .btn-ghost:hover {
      border-color: #3a4d50;
    }

    /* SECTIONS */

    .section {
      padding: 3.5rem 0;
    }

    .section-alt {
      background: radial-gradient(
        140% 140% at 0% 0%,
        rgba(255, 255, 255, 0.02),
        transparent 60%
      );
    }

    .section-header {
      text-align: center;
      max-width: 40rem;
      margin: 0 auto 2.2rem;
    }

    .section-header.left {
      text-align: left;
      margin-left: 0;
    }

    .section-header h2 {
      margin: 0;
      font-family: Poppins, Inter, sans-serif;
      font-size: 1.7rem;
      color: var(--snow-cream);
    }

    .section-header p {
      margin-top: 0.7rem;
      color: var(--text-default);
      font-size: 0.95rem;
    }

    /* SERVICES GRID */

    .grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 1.35rem;
    }

    .card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius-lg);
      padding: 1.2rem 1.3rem 1.3rem;
      box-shadow: var(--shadow-lg);
    }

    .card h3 {
      margin: 0 0 0.4rem;
      font-size: 1.05rem;
      color: var(--snow-cream);
    }

    .card p {
      margin: 0 0 0.7rem;
      font-size: 0.9rem;
      color: var(--text-default);
    }

    .chip-list {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
      font-size: 0.75rem;
    }

    .chip-list li {
      padding: 0.15rem 0.5rem;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: #132423;
      color: var(--moss-200);
    }

    /* PROCESS */

    .steps {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1.5rem;
      font-size: 0.92rem;
    }

    .steps h3 {
      margin: 0 0 0.25rem;
      font-size: 1rem;
      color: var(--snow-cream);
    }

    .steps p {
      margin: 0;
      color: var(--text-default);
    }

    /* ABOUT */

    .about-grid {
      display: grid;
      grid-template-columns: minmax(0, 2fr) minmax(0, 1.3fr);
      gap: 2rem;
      align-items: flex-start;
    }

    .about-grid p {
      font-size: 0.95rem;
    }

    .about-aside {
      background: var(--card);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border);
      padding: 1.2rem 1.4rem;
      box-shadow: var(--shadow-lg);
      font-size: 0.92rem;
    }

    .about-aside h3 {
      margin-top: 0;
      font-size: 1rem;
    }

    .about-aside ul {
      list-style: disc;
      padding-left: 1.2rem;
      margin: 0.4rem 0 0;
    }

    /* CONTACT */

    .contact-inner {
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 1.4fr);
      gap: 2rem;
      align-items: flex-start;
    }

    .contact-form {
      background: var(--card);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border);
      padding: 1.4rem 1.5rem;
      box-shadow: var(--shadow-lg);
      font-size: 0.9rem;
    }

    .field {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      margin-bottom: 0.9rem;
    }

    .field label {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--text-muted);
    }

    .field input,
    .field textarea {
      border-radius: 0.6rem;
      border: 1px solid var(--border);
      padding: 0.55rem 0.7rem;
      background: #0e1718;
      color: var(--text-strong);
      font: inherit;
    }

    .field input:focus,
    .field textarea:focus {
      outline: 1px solid var(--ember-500);
      outline-offset: 1px;
      border-color: var(--ember-500);
    }

    .btn-primary.full {
      width: 100%;
      margin-top: 0.2rem;
    }

    .contact-note {
      font-size: 0.8rem;
      color: var(--text-muted);
      margin-top: 0.6rem;
    }

    .contact-note a {
      color: var(--moss-200);
    }

    .contact-note a:hover {
      color: var(--snow-cream);
    }

    /* FOOTER */

    .site-footer {
      border-top: 1px solid var(--border);
      padding: 1.4rem 0 1.8rem;
      font-size: 0.8rem;
      color: var(--text-muted);
    }

    .footer-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
    }

    .footer-small {
      opacity: 0.85;
    }

    /* RESPONSIVE */

    @media (max-width: 960px) {
      .nav {
        display: none;
      }

      .header-inner {
        justify-content: space-between;
      }

      .grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .steps {
        grid-template-columns: minmax(0, 1fr);
      }

      .about-grid,
      .contact-inner {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    @media (max-width: 640px) {
      .section {
        padding: 2.6rem 0;
      }

      .hero {
        padding: 3.5rem 0 2.6rem;
      }
    
      .hero-logo {
        width: min(280px, 75vw);
        margin-bottom: 1.1rem;
      }

      .section-header h2 {
        font-size: 1.5rem;
      }

      .grid {
        grid-template-columns: minmax(0, 1fr);
      }

      .footer-inner {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  </style>

</html>
